# Rust build output
/target

# Release staging
/dist

# PHP dev tooling (PHPUnit)
/vendor
/.phpunit.cache

# Editor / OS noise
*.swp
.DS_Store

# Claude Code local settings & worktrees
/.claude

# Local design notes (kept out of git by request)
/docs/superpowers

# Benchmark: composer deps, generated fixtures, and machine-specific results
/bench/vendor
/bench/fixtures
/bench/results.json
